<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="gpradoT5A.Views.ActualizarPersona">
    <VerticalStackLayout>
        <Label 
            Text="Actualizar Persona"
            HorizontalTextAlignment="Center"
            HorizontalOptions="FillAndExpand"
            FontSize="25"
            Background="DeepSkyBlue" />

        <Grid Padding="5" RowSpacing="1" ColumnSpacing="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Entry x:Name="txtNombre" Placeholder="Ingrese nuevo nombre" Grid.Row="0"/>

            <Button x:Name="btnGuardar" Text="Guardar" Clicked="btnGuardar_Clicked" Grid.Row="1" BackgroundColor="Blue" TextColor="White" />

            <Label x:Name="lblStatus" TextColor="Red" Grid.Row="2"/>

        </Grid>
    </VerticalStackLayout>
</ContentPage>